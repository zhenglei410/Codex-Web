# Docker Compose / 生产环境示例（复制成 .env 后修改）

# API Key 的加密主密钥：请用 openssl rand -hex 32 生成，并单独保管
# 不设置时会使用容器内 data/secrets.key（也能工作，但备份时要一起备份）
CODEX_WEB_SECRET_KEY=

# 需要 Codex Web 管理的宿主机端口
CODEX_WEB_PORT=127.0.0.1:8790

# 工作目录（挂载进容器的 /workspace，网页里选择该路径）
CODEX_WEB_WORKSPACE=./workspace
