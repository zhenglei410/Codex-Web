# 安全策略

## 项目定位

Codex Web 让你在浏览器里驱动**本机**的 Codex CLI。它天生就是一个「能远程操作服务器」的工具：
能登录的人就能以服务运行用户的身份执行命令、读写文件。请始终按这个前提部署。

## 部署基线

- 不要把服务端口直接暴露到公网，只经 nginx/HTTPS 反代；
- 使用专用系统用户运行服务，并把「允许的工作目录」限制到必要范围；
- 成员账号默认给 `read-only` 或 `workspace-write`，并开启审计日志；
- `config.json`、`data/secrets.key`、`data/audit.jsonl` 保持 `600` 权限；
- 需要真正隔离时使用容器或虚拟机，只挂载工作目录（见 docs/deployment.md）。
- 首次部署的默认账号是 `admin` / `admin`，服务会强制要求首次登录改密；
  **在改密之前不要把服务暴露到公网**（先在内网/本地登录并改掉密码，再开放端口更稳妥）。

## 已知的安全边界

1. 「本机权限控制」是产品层面的护栏（目录白名单、命令黑名单、受保护路径、超时、审计），
   **不是操作系统级沙箱**，正则黑名单可被绕过；受保护路径是在检测到文件变更事件后终止任务。
2. 服务不做多租户隔离：所有账号共用同一个 Codex 安装与 `CODEX_HOME`，会话记录文件在磁盘上也是共享的
   （项目通过 `data/thread-owners.json` 在应用层做了归属过滤）。
3. API Key 以 AES-256-GCM 密文保存，主密钥默认在 `data/secrets.key`。只拿到 `config.json`
   无法还原密钥，但拿到同一台机器上的 `data/` 目录就可以 —— 请按同样的标准保护这两个文件。
4. 服务依赖 nginx 透传的 `X-Forwarded-Proto` 判断是否加 `Secure` Cookie；请勿让客户端直接访问后端端口。

## 报告漏洞

如果你发现了安全问题，请不要在公开 Issue 中披露细节，通过以下方式私下联系：

- 在 GitHub 上使用仓库的 **Security → Report a vulnerability**（Private vulnerability reporting）；
- 或在 Issue 里只留一句「需要私下沟通安全问题」，维护者会主动联系。

请在报告中包含：影响版本、复现步骤、影响范围、可能的修复思路。
